name: "Update wys.dyn"
on:
  schedule:
  - cron: "0 0 * * *"

jobs:
  run_tests:
    runs-on: ubuntu-22.04
    steps:
    - uses: actions/checkout@v2
    - name: Run script file
      run: |
         chmod +x ./scripts/wys.dyn_update.sh
         ./scripts/wys.dyn_update.sh
      shell: bash
